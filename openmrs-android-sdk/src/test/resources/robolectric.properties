# Robolectric emulates the module's targetSdk, and falls back to compileSdk when a library
# module declares none. compileSdk is 36, which this Robolectric version predates, so the
# Android runtime it builds is incomplete and OkHttp's platform probe fails against it.
# Pin an SDK level this Robolectric fully supports; it only affects unit tests.
sdk=34
